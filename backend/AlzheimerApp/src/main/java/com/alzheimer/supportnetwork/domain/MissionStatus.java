package com.alzheimer.supportnetwork.domain;

/** Lifecycle state for a support mission assigned to a network member. */
public enum MissionStatus {
    PENDING,
    ACCEPTED,
    COMPLETED,
    CANCELLED
}
